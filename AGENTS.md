# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# TypeScript types

Avoid non-primitive unions (e.g. discriminated unions of object shapes) wherever possible. Prefer simpler, flatter prop/type shapes instead.
